<div class="event">
  <div class="event-info row">
    <div class="col-md-12">
      <%= content_tag 'div', '', class: 'event-background',
                      style: "background-image: url(#{asset_pack_path('media/images/event.jpg')})" %>

      <div class="event-info-inner row">
        <div class="col-md-6">
          <div class="event-description">
            <h1 class="buffer-top"><%= @event.title %></h1>
            <h3><%= l @event.datetime, format: :long %></h3>
            <h3><small><%= link_to @event.address,
                                   "https://yandex.ru/maps/?text=#{@event.address}" %></small></h3>
            <p><%= @event.description %></p>
          </div>

          <% if current_user_can_edit?(@event) %>
            <div class="buffer-top">
              <%= link_to t('pages.events.show.button.change'), edit_event_path(@event),
                          class: 'btn btn-dark buffer-both' %>
              <%= link_to t('global.forms.delete'), @event, method: :delete,
                          class: 'btn btn-danger buffer-both',
                          data: { title: t('global.confirm.consent'),
                                  confirm: t('global.confirm.warning'),
                                  commit: t('global.confirm.ok'),
                                  cancel: t('global.confirm.cancel') } %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-md-8 rounded">
      <div class="event-comments buffer-top">

        <%= render partial: 'comments/form',
          locals: {comment: @new_comment || @event.comments.build} %>

        <div class="buffer-both">
          <%= render partial: 'comments/comment',
            collection: @event.comments.except(@new_comment) %>
        </div>
      </div>
    </div>

    <div class="col-md-4">
      <div class="event-owner">
        <h4><%= t('pages.events.show.static.calling') %></h4>
        <p><%= render partial: 'users/user', object: @event.user %></p>
      </div>
      <div class="event-subscriptions">
        <h4><%= t('pages.events.show.static.will_go') %></h4>
        <div>
          <p>
          <%= render partial: 'subscriptions/subscription',
            collection: @event.subscriptions.except(@new_subscription) %>
          </p>
        </div>
      </div>
      <% #if @event.user != current_user %>
      <div class="event-subscription-form">
        <h4><%= t('pages.events.show.static.i_will_go') %></h4>
        <div>
          <%= render partial: 'subscriptions/form', locals: {
            subscription: @new_subscription || @event.subscriptions.build
          } %>
        </div>
      </div>
      <% #end %>
    </div>

  </div>

</div>
